#include <stdio.h>
#include <stdlib.h>
#include <array.h>
#include <strlib.h>

int main(int argc, arr(str) argv)
{
    int n = argc-1;

    arr(int) a = build(argv, n);

    // do something with a!
    selectionsort( a , n ) ;

    printa(a, n);

    return 0;
}